[package]
name = "agent_core"
version = "0.1.0"
edition.workspace = true
description = "Protocol-agnostic agent domain types — the universal vocabulary for messages, tasks, and artifacts independent of A2A, MCP, or any wire protocol"

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
