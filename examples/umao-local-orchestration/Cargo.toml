[package]
name = "umao-local-orchestration"
version = "0.1.0"
edition = "2024"
publish = false
description = "UMAO local orchestration demo — two A2A agents + graph-driven orchestrator."

[[bin]]
name = "researcher"
path = "src/researcher.rs"

[[bin]]
name = "synthesizer"
path = "src/synthesizer.rs"

[[bin]]
name = "orchestrator"
path = "src/main.rs"

[dependencies]
umao_core = { workspace = true }
umao_executor = { workspace = true }
umao_agents = { path = "../../crates/umao_agents" }
a2a_http_server = { workspace = true }
a2a_protocol_core = { workspace = true }
agent_sdk = { workspace = true, features = ["a2a-client"] }

serde_json = "1"
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = "0.3"
clap = { version = "4", features = ["derive"] }
